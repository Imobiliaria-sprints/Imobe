import { CreateUserUseCase } from "@cases/CreateUserUseCase/CreateUserUseCase";
import { GenerateTokenProvider } from "src/provider/GenerateTokenProvider";

let createUserUseCase: CreateUserUseCase;
let tokenProvider: GenerateTokenProvider;

beforeAll(() => {});

describe("Genete Token", () => {});
